version: "3"

vars:
  TOOL_PATH:
    sh: printf '%s' "$HOME/.local/bin:$HOME/.cargo/bin:$HOME/go/bin:$PATH"

env:
  PATH: "{{.TOOL_PATH}}"

tasks:
  default:
    cmds:
      - task --list
    silent: true

  setup:
    desc: Install dependencies and browser runtimes
    cmds:
      - npm ci
      - |
        if [ "$(uname -s)" = "Linux" ]; then
          npx playwright install --with-deps chromium webkit
        else
          npx playwright install chrome webkit
        fi

  test:
    desc: Run contract tests across Node, Bun, Deno, Chromium/Chrome, and WebKit
    cmds:
      - npx node --test tests/count.test.js tests/browser.test.js
      - npx bun test tests/bun.test.js
      - npx deno test --allow-read tests/deno.test.js

  coverage:
    desc: Run runtime matrix plus Node coverage (100% threshold)
    cmds:
      - npx bun test tests/bun.test.js
      - npx deno test --allow-read tests/deno.test.js
      - >-
        npx node --test --experimental-test-coverage
        --test-coverage-include='src/**'
        --test-coverage-lines=100
        --test-coverage-branches=100
        --test-coverage-functions=100
        tests/count.test.js tests/browser.test.js

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf node_modules
